[files]
# Path to the extracted WSO2 IS folder.
is_folder_path=/Users/pasindu/project/is-packs/patch/consent/wso2is-7.1.0

# --- Legacy zip-based options (use is_folder_path above instead) ---
# is_already_unzipped=false
# zip_file_path=/Users/pasindu/project/is-packs/older-packs/wso2is-7.1.0.zip
# unzip_dir_path=/Users/pasindu/project/is-packs/patch/consent
# is_folder_name=wso2is-7.1.0 # Zip file name without the extension is taken by default.
# {unzip_dir_path}/{is_folder_name}

[database]
type=oracle # mysql, postgresql, mssql or db2
; version=8.0 # Optional: Docker image tag (e.g. 8.0)Uses latest if not set or if the image is unavailable.
; container_name=mysql_8_0 # Optional: Custom container name. Uses default name based on DB type if not set.
password=myStrongPaas42emc2
identity_db_name=WSO2_IDENTITY_DB
shared_db_name=WSO2_SHARED_DB
enable_pooling=false
force_container_recreation=false

[server]
run_is=false
run_in_debug=false
